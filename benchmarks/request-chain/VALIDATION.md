# 内部软件验证范围

本批仍为 AI 起草、待人工核验。以下结果只检查既有软件对受控输入的处理；不是人工标签正确率、真实模型成绩或全技术栈兼容承诺。

| 层次 | 实际覆盖 | 没有覆盖的结论 |
| --- | --- | --- |
| 纯结构与规则 | 18 候选、36 答案标签；6 家族 × 3 档；逐题引用绑定、条件变化和仅改名反例；19 检查通过 | 引用契约不能证明教学语义正确或档位适当 |
| 独立 API | 18 候选全部档位，真实入口、队列、受控 TLS、正式提交、相关性与评分、10 点奖励、单次 pass 仍 unverified | 模型回复是受控见证，不能证明真实模型能生成或判准 |
| 普通 API | 6 家族的基础档；错误但相关的原答仍获同轮 10 点，反馈保明确短板 | 未将该路径外推到全部档位 |
| 陌生性与帮助 API | systems.cost-1、frontend.state-1、network.trace-1；原题后仅改标题被拒，条件变式接受，确认不等交付，方向帮助交付后按练习记录 | 另外三个家族与高档帮助路径未作本批真实流程矩阵验收 |
| 浏览器 | 同上述三个基础案例；键盘作答、普通反馈、主动独立变式、方向提示确认与渲染回执、练习反馈、返回原轮保留原答、320px 和 200% 字号无横溢 | 未测全部语言/框架、全档位或真人理解效果 |

实际背景为 systems-evidence-v1、frontend-evidence-v1、network-evidence-v1；案例绑定 fullstack-v1.0.0 的六个明确主能力。UI 流程没有把题面提到的其他组件记为通过。

来源文件保留官方短摘录与定位。测试服务器通过本地受控 TLS 转发这些摘录；其 URL 明确属于受控服务器，绝不伪装官方站点。教学时序、日志、资源数值与模型回复均为合成输入。测试没有真实付费模型调用、真实邮件发送或部署。

## 命令与结果口径

从 backend 目录运行，专属环境设置 MAILPIT_URL 与 NO_PROXY；浏览器另设置 FRONTEND_HOST、PLAYWRIGHT_BASE_URL，均指向本票本机服务。

- `../.venv/bin/pytest tests/test_request_benchmarks.py --noconftest -q`：19 通过，含引用修复后检查。
- `../.venv/bin/pytest tests/test_request_benchmark_api.py -q`：27 个用例。冻结前独立 18、普通及帮助 9 已分组通过；引用修复后相关 execution/network 13 用例再次通过。完整后端回归结果以实际最终日志为准，不将分组结果称为同次全量结果。
- `../.venv/bin/python -m tests.request_benchmarks_browser`：三个基础案例分别运行一个真实浏览器用例；均通过。harness 结束时停止自己的 worker/provider，并把截图复制到 `/tmp/bp-issue-35-artifacts`。

本批专用 spec 在没有明确 harness 环境时跳过，不改变公共 CI、产品 API 或 worker。可复用现有测试入口运行本批用例；没有另建评测 runner 或第二套评分实现。

剩余交付需要真人逐条核验案例/标签/变式/帮助与版本来源，并在明确授权后用真实模型取得质量证据。本次只能部分关联 #35，不能关闭该 Issue。
